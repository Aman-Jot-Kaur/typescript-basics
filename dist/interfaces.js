"use strict";
class Instagram {
    constructor(mode, filter, burst, short // we can add extra things but must add all from interface
    ) {
        this.mode = mode;
        this.filter = filter;
        this.burst = burst;
        this.short = short;
    }
    create() { }
}
